# ones-AI 平台开发任务追踪

更新时间: 2026-03-18 20:00

## 一、通知系统 (已完成)
- [x] Webhook + Email 双通道通知
- [x] 管理界面配置 UI

## 二、Runner Docker 执行改造 (已完成)
- [x] 改用 `ones-AI` 脚本走 Docker
- [x] SSH 命令 UTF-8 locale 设置

## 三、SSH / DB 修复 (已完成)
- [x] `ssh_pool.py` is_closed 兼容
- [x] `task_executor.py` stderr await
- [x] `notifications.py` 列名修复

## 四、Agent 目录 + 挂载路径 + 提示词注入 (已完成)
- [x] 前端 TaskView.vue 新增 Agent 目录必填框
- [x] 前端每工单额外挂载路径（多个、可展开）
- [x] 前端敏感路径过滤（BLOCKED_PATHS）
- [x] 后端 tasks.py 新增字段 + validate_path()
- [x] 后端 task_executor.py 传参 --agent-dir + --extra-mounts
- [x] 后端 database.py schema + DB 迁移
- [x] ones-AI 脚本挂载改造（Docker -v agent_dir + extra_mounts）
- [x] Runner 提示词注入（load_agent_prompts + prompt 前置）

## 五、任务模式 + Patch 回收 (已完成，待部署)
- [x] 前端任务模式选择器（fix/analysis/auto）
- [x] 后端 task_mode 字段 + 校验 + INSERT
- [x] Runner MODE_PROMPTS 字典 + 模式注入
- [x] Runner git diff 回收

## 六、ARG_MAX Bug 修复 (已完成，待部署)
- [x] Runner execute() 改用 stdin 管道传 prompt（Popen + communicate）
- [x] 超时 proc.kill() 清理

## 七、首次加载性能优化 (已完成，待部署)
- [x] vite.config.js manualChunks 分包
- [x] nginx.conf gzip 压缩 + 静态资源缓存

## 八、打回重做功能 (已完成，待部署)
- [x] 前端 TaskDetailView 打回重做按钮 + 对话框
- [x] API reworkTicket 方法
- [x] 后端 rework 端点（创建新 ticket 注入上次结果）

## 九、Runner 产出展示增强 (待做)
- [ ] 增强 JSON 输出字段
- [ ] 前端结构化展示
- [ ] 邮件整合
